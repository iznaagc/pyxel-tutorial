# プロジェクト変更履歴 / Project History Log

## [2026-03-10] プロジェクトの初期セットアップ
- **変更ファイル:** `.gitignore`, `Rule.md`, `AGENT.md`, `src/main.py`, `requirements.txt`
- **概要:** Python 3.14 を使用して仮想環境 (`.venv`) を構築しました。除外ファイル設定 (`.gitignore`)、プロジェクトルール (`Rule.md`)、AI向けの指示書 (`AGENT.md`) を作成し、Pyxel 用の最小構成の雛形 `main.py` を用意しました。また、プロジェクト依存パッケージとして `pyxel` と `pyxel-mcp` をインストールしました。

## [2026-03-10] HISTORY.md による履歴追跡ルールの追加
- **変更ファイル:** `AGENT.md`, `HISTORY.md`
- **概要:** AI アシスタントに対して、行ったすべての作業や変更をこの `HISTORY.md` に記録するように指示する新しいルールを追加しました。

## [2026-03-10] 倉庫番 Step 1: 状態遷移の土台作成
- **変更ファイル:** `src/main.py`
- **概要:** `main.py` をリファクタリングし、シーン管理システム (タイトル, プレイ, クリア) を導入しました。ゲーム要素を追加する前に状態遷移のロジックを確認するため、キーボード入力 (ENTER, C, R) による基本的な画面遷移を実装しました。

## [2026-03-10] 倉庫番 Step 2: マップ描画とプレイヤー移動
- **変更ファイル:** `src/main.py`
- **概要:** 文字列による `LEVEL_1` マップデータを追加しました。そのデータを `walls`（壁）、`goals`（ゴール）、`boxes`（箱）、`player`（プレイヤー）へと解析・パースするロジックを実装。基本図形（四角や円）を使った描画関数を追加し、矢印キーによるプレイヤー移動と壁への当たり判定を実装しました。

## [2026-03-10] 倉庫番 Step 3 & 4: 箱の移動ロジックとクリア判定
- **変更ファイル:** `src/main.py`
- **概要:** 箱を押し込む仕組み（箱の移動先に壁や別の箱がない場合のみ移動可能とする）を追加しました。すべての箱の座標がゴールの座標と一致しているか確認し、一致していればクリア状態 (`SCENE_CLEAR`) に遷移する勝利条件のチェックロジックを実装しました。

## [2026-03-10] ドキュメントの日本語化とルールの追加
- **変更ファイル:** `Rule.md`, `AGENT.md`, `HISTORY.md`
- **概要:** すべてのドキュメントおよび `HISTORY.md` の記録を日本語に翻訳しました。また、以後のAIにおけるやり取りや記録はすべて日本語で行うという明示的なルールを追加しました。

## [2026-03-10] 倉庫番 Step 5: ドット絵と効果音によるブラッシュアップ
- **変更ファイル:** `src/main.py`
- **概要:** `pyxel.image(0).set()` を用いて、プレイヤー、壁、床、ゴール、箱の8x8の基本的なドット絵グラフィック（ピクセルアート）を実装しました。また `pyxel.sound().set()` を用いて、移動時、箱の押し込み時、クリア時の簡単な効果音を追加し、プレイのフィードバックを強化しました。

## [2026-03-10] 共通処理と各シーン（画面）のモジュール分割（リファクタリング）
- **変更ファイル:** `src/main.py`, `src/constants.py`, `src/assets.py`, `src/scenes/title.py`, `src/scenes/play.py`, `src/scenes/clear.py`
- **概要:** 単一の肥大化した `main.py` を複数の意味あるモジュールに分割しました。
  - 定数やマップ構造を `constants.py` に分離
  - 画像と音声のセットアップ処理を `assets.py` に分離し、あわせて表示されていた非推奨APIの警告 (`pyxel.image()` ➔ `pyxel.images[]`) に対応
## [2026-03-10] 拡張機能１：複数ステージとUndo（1手戻す）機能の追加
- **変更ファイル:** `src/constants.py`, `src/main.py`, `src/scenes/play.py`, `src/scenes/clear.py`
- **概要:** リファクタリングされたベースを活用し、倉庫番としてのゲーム性を高める機能を実装しました。
  - `constants.py` に `LEVELS` 配列を定義し、それぞれ難易度の違う3つのステージを用意しました。
## [2026-03-10] レベルデザインルールの追加とマップ修正
- **変更ファイル:** `Rule.md`, `src/constants.py`
- **概要:** ユーザーのご指摘により、ステージ２(`LEVEL_2`)において「ゴールの数が3つ、箱の数が2つ」と不一致になっておりプレイヤーが混乱する状態だったものを修正しました。
  - `constants.py` 内の `LEVEL_2` の余分なゴール（.）を削除し、箱とゴールの数を一致させました。
  - `Rule.md` に **「マップを作成する際、箱（$）とゴール（.）の数は必ず同数にする」** というステージ設計の厳格なルールを追記しました。

## [2026-03-10] プロジェクトの最終ドキュメント作成と完了
- **変更ファイル:** `documents/*.md`, `README.md`, `HISTORY.md`
- **概要:** チュートリアルの完了に伴い、プロジェクトの全体像をまとめた `README.md` を作成しました。また、`src` 以下の各モジュールの役割を詳細に解説したドキュメントを `documents/` ディレクトリ配下に整備しました。これにより、後からコードを見返した際の保守性が向上しました。
